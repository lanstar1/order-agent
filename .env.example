# ── Claude AI API 키 (필수) ──────────────────────────
# https://console.anthropic.com 에서 발급
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# ── ECOUNT ERP 설정 ──────────────────────────────────
ERP_COM_CODE=89356
ERP_USER_ID=TIGER
ERP_ZONE=CD
ERP_API_KEY=1d667e0cff97845728acdeed64e34ce789
ERP_WH_CD=10

# ── 로젠택배 (SmartLogen) ──────────────────────────
LOGEN_YONGSAN_ID=
LOGEN_YONGSAN_PW=
LOGEN_GIMPO_ID=
LOGEN_GIMPO_PW=

# ── Google (선택) ──────────────────────────────────
GOOGLE_API_KEY=
GOOGLE_SERVICE_ACCOUNT_JSON=
GOOGLE_CS_FOLDER_ID=

# ── 서버 설정 ──────────────────────────────────────
HOST=0.0.0.0
PORT=8000
DEBUG=false

# ── AI 파라미터 ──────────────────────────────────────
CONFIDENCE_THRESHOLD=0.90

# ── 네이버 커머스 API (스마트스토어) ──────────────────
NAVER_COMMERCE_CLIENT_ID=
NAVER_COMMERCE_CLIENT_SECRET=

# ── 스마트스토어 ERP 연동 ────────────────────────────
SMARTSTORE_CUST_CODE=
SMARTSTORE_EMP_CODE=
SMARTSTORE_WH_CODE=30

# ── 데이터베이스 ──────────────────────────────────────
# NAS 배포: DATABASE_URL 미설정 → 자동으로 SQLite 사용
# Render 배포: DATABASE_URL=postgresql://... 설정
# DATABASE_URL=

# ── 신제품 소싱 모듈 (v0.3.0) ────────────────────────
# YouTube Data API v3 (Google Cloud Console에서 발급)
YOUTUBE_API_KEY=
# OpenAI (자막 보정 LLM용 gpt-4o-mini)
OPENAI_API_KEY=
# 네이버 검색 + DataLab 쇼핑인사이트 (동일 Client ID/Secret 공유)
NAVER_SEARCH_CLIENT_ID=
NAVER_SEARCH_CLIENT_SECRET=
# 네이버 검색광고 API (광고주 계정, HMAC-SHA256)
NAVER_AD_API_KEY=
NAVER_AD_CUSTOMER_ID=
NAVER_AD_SECRET_KEY=
# 실패 알림 Slack Incoming Webhook (선택)
SOURCING_SLACK_WEBHOOK=
# 스케줄러 틱 간격 (자막 미생성 영상 재시도 주기)
SOURCING_POLL_INTERVAL_HOURS=3
# ── 자막 수집 IP 차단 우회 ────────────────────────
# 선택 1: YouTube cookies.txt 파일 내용 (Netscape 형식, 전체 복붙)
# 브라우저 확장 'Get cookies.txt LOCALLY' 로 youtube.com 접속 상태에서 추출
# 보안: 세션 탈취 위험 있으니 전용 계정 권장
YOUTUBE_COOKIES_TEXT=
# 선택 2: 쿠키 파일 경로가 있다면 (위 TEXT 보다 우선)
YOUTUBE_COOKIES_FILE=
# 선택 3: Webshare residential proxy (월 $1~, 자동 rotating)
YT_TRANSCRIPT_PROXY_USERNAME=
YT_TRANSCRIPT_PROXY_PASSWORD=
